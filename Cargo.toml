[package]
name = "nyth"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
rustix = { version = "1", features = ["mount", "fs", "process"] }
serde = { version = "1", features = ["derive"] }
toml = "1"
